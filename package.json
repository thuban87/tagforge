{
  "name": "tagforge",
  "version": "1.0.0",
  "description": "Automatic hierarchical tag management for Obsidian",
  "main": "main.js",
  "scripts": {
    "dev": "node esbuild.config.mjs",
    "build": "node esbuild.config.mjs production",
    "deploy:test": "powershell -Command \"$dest='C:\\Quest-Board-Test-Vault\\.obsidian\\plugins\\tagforge'; if(!(Test-Path $dest)){New-Item -ItemType Directory -Path $dest -Force}; Copy-Item main.js,manifest.json,styles.css -Destination $dest -Force; Write-Host '✅ Deployed to test vault'\"",
    "deploy:production": "node deploy-production.mjs"
  },
  "keywords": [
    "obsidian",
    "plugin",
    "tags",
    "automation"
  ],
  "author": "Brad",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.10.0",
    "esbuild": "^0.19.11",
    "obsidian": "latest",
    "typescript": "^5.3.3"
  }
}